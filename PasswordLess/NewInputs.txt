Inputs:
Auth<device_info>

User_Passkey_History[<device_info>, passkey_type]


<device_info>:= [OS name, OS version, Client name, Client version, IP, device_size, Device ID]

Passkey_type:= cloud | device / local | NoPasskey

** device ID: is nullable when cookie is not present | or available ]

Action:
Determine if auth<device_info> match any of user_passkey_history

Outputs:
Match_probability := 0 {least likely} to 100 {most likely}
Match_passkey_type := the matching passkey type  [clooud | device | local]